[package]
name = "todo-rocket"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5.0-rc.2", features = [ "json" ] }
todo-logic ={ path = "../todo-logic" }
log = "0.4"
simplelog= "0"
